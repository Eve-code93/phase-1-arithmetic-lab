// Define two numbers whose product is 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // 31 * 2 = 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Ensures a number > 0

// Define numbers for modulo operation
const num3 = 14;
const num4 = 5;
const mod = num3 % num4; // 14 % 5 = 4

// Find the maximum number from a set
const max = Math.max(10, 5, 20, 15); // Returns 20
